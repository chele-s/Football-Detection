model:
  path: "models/checkpoint_best_total_480_rfdetr.onnx"  # ONNX model for 1.5-2x speedup
  confidence: 0.12
  iou_threshold: 0.45
  device: "cuda"
  half_precision: true
  imgsz: 480
  multi_scale: false
  warmup_iterations: 3
  ball_class_id: 0

tracking:
  max_lost_frames: 15
  min_confidence: 0.1
  iou_threshold: 0.15
  history_size: 30
